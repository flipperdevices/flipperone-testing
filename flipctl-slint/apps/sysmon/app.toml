# The system monitor, drawing its own four pages.
#
# A Wayland client on the compositor flipctl runs. The rows and the graphs are
# flipctl's own widgets, from the framework in crates/flipctl-app, so the app looks
# like the device without anything passing between them.
name = "System Monitor"
wayland = "./target/release/sysmon-app"
